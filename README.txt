# DWI Processing

The script `process.sh` will process single-shell reverse phased DWI into GM, CSF, and WM-FOD images suitable for use in tractography. 

Run ./install before first running the script.

